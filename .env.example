# OpenAI API key (required for cheap / premium mode)
OPENAI_API_KEY=sk-your-api-key-here

# LLM mode: cheap (gpt-4.1-nano) | premium (gpt-4.1-mini) | mock (offline, no API key needed)
LLM_MODE=cheap

# Debug mode: true | false — when true, logs full prompt/response to console
DEBUG_LLM=false
